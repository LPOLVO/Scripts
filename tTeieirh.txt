-- [[ LP10 HUB | PART 1 ]]
if not game:IsLoaded() then
    game.Loaded:Wait()
end

if _G.IsOpened then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "LP10 HUB",
        Text = "السكربت خدام ديجا آ حمزة، تبارك الله عليك",
        Duration = 5
    })
    local sound = Instance.new("Sound", game.Workspace)
    sound.SoundId = "rbxassetid://17692186249"
    sound.Volume = 10
    sound:Play()
    return
else
    _G.IsOpened = true
end

-- [ PERSONAL INFO ]
local Dev = "@LPOLVO"
local User = "Hamza"
if setclipboard then
    setclipboard("https://www.instagram.com/4me.h4mz4_?igsh=MW9sa20yejExaGo5Nw==")
end

-- [[ THEME CONFIGURATION (CYAN) ]]
local Cyan = Color3.fromRGB(0, 255, 255)
local DarkCyan = Color3.fromRGB(0, 100, 100)

-- استدعاء المكتبة (Library) مع تبديل الألوان لـ Cyan
local vu31, vu32, vu33 = 0, 255, 255
local v34, v35, v36 = 0, 50, 50

-- [ بداية تعريف المتغيرات الأساسية ]
local v37 = game.Players.LocalPlayer
local v38 = v37:GetMouse()
local v39 = game:GetService("TweenService")
local v40 = game:GetService("HttpService")
-- [[ LP10 HUB | PART 2 ]]

local v41 = game:GetService("VirtualUser")
local v42 = game:GetService("MarketplaceService")
local v43 = game:GetService("RunService")

-- إعدادات الألوان (Cyan Theme)
local v44 = Color3.fromRGB(0, 255, 255) -- Cyan
local v45 = Color3.fromRGB(0, 150, 150) -- Dark Cyan
local v46 = Color3.fromRGB(20, 20, 20)  -- Background

-- جداول الإعدادات (Tables)
local LP10_Settings = {
    AutoFarm = false,
    KillAura = false,
    Speed = 16,
    Jump = 50,
    ThemeColor = v44
}

-- [ الروابط والمعلومات الشخصية ]
local LP10_Discord = "discord.gg/VmJUrmzaF"
local LP10_Insta = "https://www.instagram.com/4me.h4mz4_/"

-- وظيفة جلب أسماء اللاعبين
local function GetPlayers()
    local players = {}
    for i, v in pairs(game.Players:GetPlayers()) do
        if v ~= v37 then
            table.insert(players, v.Name)
        end
    end
    return players
end

-- [ بداية تعريف الـ UI Elements ]
local LP10_Gui = Instance.new("ScreenGui")
LP10_Gui.Name = "LP10_HUB_GUI"
LP10_Gui.Parent = game.CoreGui
LP10_Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = LP10_Gui
MainFrame.BackgroundColor3 = v46
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -300, 0.5, -200)
MainFrame.Size = UDim2.new(0, 600, 0, 400)
-- [[ LP10 HUB | PART 3 ]]

local function GetCharacter()
    return v37.Character or v37.CharacterAdded:Wait()
end

local function GetRoot(char)
    return char:FindFirstChild("HumanoidRootPart") or char:WaitForChild("HumanoidRootPart")
end

-- وظيفة الانتقال (Teleport) بأسلوب LP10
local function LP10_Teleport(cframe)
    local char = GetCharacter()
    if char then
        local root = GetRoot(char)
        if root then
            root.CFrame = cframe
        end
    end
end

-- وظيفة الإشعارات داخل السكربت (Custom Notifications)
local function SendLP10Message(title, text)
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "💎 LP10: " .. title,
        Text = text,
        Icon = "rbxassetid://6031064398",
        Duration = 5
    })
end

-- [[ تهيئة الألوان للمكتبة - Cyan Edition ]]
local UI_Colors = {
    Primary = Color3.fromRGB(0, 255, 255),
    Secondary = Color3.fromRGB(0, 150, 150),
    Text = Color3.fromRGB(255, 255, 255),
    Background = Color3.fromRGB(15, 15, 15)
}

-- محاكاة الضغط (Input Simulation)
local function PressKey(keycode)
    v41:CaptureController()
    v41:ClickButton2(Vector2.new())
end

-- [ بداية الـ Loop الخاص بالـ Auto Equip اللي بغيتي ]
task.spawn(function()
    while true do
        if LP10_Settings.AutoEquip then
            local char = GetCharacter()
            if char and not char:FindFirstChildOfClass("Tool") then
                game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.One, false, game)
                task.wait(0.01)
                game:GetService("VirtualInputManager"):SendKeyEvent(false, Enum.KeyCode.One, false, game)
            end
        end
        task.wait(0.5)
    end
end)
-- [[ LP10 HUB | PART 4 ]]

-- إعدادات الألوان والخطوط للـ Library
local v47 = {
    Main = Color3.fromRGB(0, 255, 255), -- Cyan
    Dark = Color3.fromRGB(15, 15, 15),
    Text = Color3.fromRGB(255, 255, 255),
    Accent = Color3.fromRGB(0, 150, 150)
}

-- إنشاء الـ Header (العنوان الفوقاني)
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Parent = MainFrame
TopBar.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BorderSizePixel = 0

local TopBarStroke = Instance.new("UIStroke")
TopBarStroke.Parent = TopBar
TopBarStroke.Color = v47.Main
TopBarStroke.Thickness = 1.5

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "TitleLabel"
TitleLabel.Parent = TopBar
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.Size = UDim2.new(0.5, 0, 1, 0)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "💎 LP10 HUB | Version 1.0"
TitleLabel.TextColor3 = v47.Main
TitleLabel.TextSize = 16
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

-- معلومات المطور فـ الـ Header
local DevLabel = Instance.new("TextLabel")
DevLabel.Parent = TopBar
DevLabel.BackgroundTransparency = 1
DevLabel.Position = UDim2.new(0.5, 0, 0, 0)
DevLabel.Size = UDim2.new(0.45, 0, 1, 0)
DevLabel.Font = Enum.Font.Gotham
DevLabel.Text = "Dev: " .. Dev
DevLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
DevLabel.TextSize = 12
DevLabel.TextXAlignment = Enum.TextXAlignment.Right

-- [ إضافة أنميشن خفيف للـ Header ]
local function PulseHeader()
    spawn(function()
        while true do
            v39:Create(TopBarStroke, TweenInfo.new(1.5), {Color = Color3.fromRGB(255, 255, 255)}):Play()
            wait(1.5)
            v39:Create(TopBarStroke, TweenInfo.new(1.5), {Color = v47.Main}):Play()
            wait(1.5)
        end
    end)
end
PulseHeader()
-- [[ LP10 HUB | PART 5 ]]

local TabContainer = Instance.new("ScrollingFrame")
TabContainer.Name = "TabContainer"
TabContainer.Parent = MainFrame
TabContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
TabContainer.BorderSizePixel = 0
TabContainer.Position = UDim2.new(0, 5, 0, 45)
TabContainer.Size = UDim2.new(0, 150, 1, -50)
TabContainer.ScrollBarThickness = 2
TabContainer.ScrollBarImageColor3 = v47.Main

local TabList = Instance.new("UIListLayout")
TabList.Parent = TabContainer
TabList.Padding = UDim.new(0, 5)
TabList.SortOrder = Enum.SortOrder.LayoutOrder

-- وظيفة إنشاء "Tab" جديد
local function CreateTab(tabName)
    local TabButton = Instance.new("TextButton")
    TabButton.Name = tabName .. "Tab"
    TabButton.Parent = TabContainer
    TabButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    TabButton.Size = UDim2.new(1, -10, 0, 35)
    TabButton.Font = Enum.Font.GothamSemibold
    TabButton.Text = tabName
    TabButton.TextColor3 = Color3.fromRGB(200, 200, 200)
    TabButton.TextSize = 14
    
    local TabCorner = Instance.new("UICorner")
    TabCorner.CornerRadius = UDim.new(0, 6)
    TabCorner.Parent = TabButton
    
    local TabStroke = Instance.new("UIStroke")
    TabStroke.Parent = TabButton
    TabStroke.Color = v47.Main
    TabStroke.Thickness = 1
    TabStroke.Transparency = 0.8

    -- تأثير عند الضغط (Hover & Click)
    TabButton.MouseButton1Click:Connect(function()
        for _, child in pairs(TabContainer:GetChildren()) do
            if child:IsA("TextButton") then
                v39:Create(child, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(200, 200, 200)}):Play()
            end
        end
        v39:Create(TabButton, TweenInfo.new(0.3), {TextColor3 = v47.Main}):Play()
        -- هنا غادي نزيدو الكود اللي كيبدل الصفحة فـ الجزء الجاي
    end)
end

-- إنشاء الأقسام الأساسية لـ LP10
CreateTab("Main")
CreateTab("Farming")
CreateTab("Combat")
CreateTab("Settings")
-- [[ LP10 HUB | PART 6 ]]

local PageContainer = Instance.new("Frame")
PageContainer.Name = "PageContainer"
PageContainer.Parent = MainFrame
PageContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
PageContainer.BorderSizePixel = 0
PageContainer.Position = UDim2.new(0, 160, 0, 45)
PageContainer.Size = UDim2.new(1, -165, 1, -50)

local PageCorner = Instance.new("UICorner")
PageCorner.CornerRadius = UDim.new(0, 8)
PageCorner.Parent = PageContainer

-- جدول باش نخزنوا كاع الصفحات
local Pages = {}

local function CreatePage(name)
    local NewPage = Instance.new("ScrollingFrame")
    NewPage.Name = name .. "Page"
    NewPage.Parent = PageContainer
    NewPage.BackgroundTransparency = 1
    NewPage.Size = UDim2.new(1, 0, 1, 0)
    NewPage.Visible = false
    NewPage.ScrollBarThickness = 3
    NewPage.ScrollBarImageColor3 = v47.Main
    
    local PageLayout = Instance.new("UIListLayout")
    PageLayout.Parent = NewPage
    PageLayout.Padding = UDim.new(0, 7)
    PageLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
    
    local PagePadding = Instance.new("UIPadding")
    PagePadding.Parent = NewPage
    PagePadding.PaddingTop = UDim.new(0, 10)

    Pages[name] = NewPage
    return NewPage
end

-- إنشاء الصفحات الأساسية لـ LP10
local MainHash = CreatePage("Main")
local FarmHash = CreatePage("Farming")
local CombatHash = CreatePage("Combat")
local SettingsHash = CreatePage("Settings")

-- وظيفة لتبديل الصفحات (Update Tab System)
local function ShowPage(name)
    for i, v in pairs(Pages) do
        v.Visible = false
    end
    if Pages[name] then
        Pages[name].Visible = true
    end
end

-- تجربة إظهار أول صفحة
ShowPage("Main")
-- [[ LP10 HUB | PART 7 ]]

local function CreateToggle(parent, text, default, callback)
    local ToggleFrame = Instance.new("TextButton")
    ToggleFrame.Name = text .. "Toggle"
    ToggleFrame.Parent = parent
    ToggleFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    ToggleFrame.Size = UDim2.new(0.9, 0, 0, 40)
    ToggleFrame.Font = Enum.Font.Gotham
    ToggleFrame.Text = ""
    ToggleFrame.AutoButtonColor = false
    
    local ToggleCorner = Instance.new("UICorner")
    ToggleCorner.CornerRadius = UDim.new(0, 6)
    ToggleCorner.Parent = ToggleFrame

    local ToggleLabel = Instance.new("TextLabel")
    ToggleLabel.Parent = ToggleFrame
    ToggleLabel.BackgroundTransparency = 1
    ToggleLabel.Position = UDim2.new(0, 10, 0, 0)
    ToggleLabel.Size = UDim2.new(0.7, 0, 1, 0)
    ToggleLabel.Font = Enum.Font.GothamSemibold
    ToggleLabel.Text = text
    ToggleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    ToggleLabel.TextSize = 14
    ToggleLabel.TextXAlignment = Enum.TextXAlignment.Left

    local Checkbox = Instance.new("Frame")
    Checkbox.Parent = ToggleFrame
    Checkbox.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Checkbox.Position = UDim2.new(1, -45, 0.5, -12)
    Checkbox.Size = UDim2.new(0, 35, 0, 24)
    
    local CheckCorner = Instance.new("UICorner")
    CheckCorner.CornerRadius = UDim.new(1, 0)
    CheckCorner.Parent = Checkbox

    local Indicator = Instance.new("Frame")
    Indicator.Parent = Checkbox
    Indicator.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    Indicator.Position = UDim2.new(0, 2, 0.5, -10)
    Indicator.Size = UDim2.new(0, 20, 0, 20)
    
    local IndicatorCorner = Instance.new("UICorner")
    IndicatorCorner.CornerRadius = UDim.new(1, 0)
    IndicatorCorner.Parent = Indicator

    local Toggled = default
    local function UpdateToggle()
        if Toggled then
            v39:Create(Indicator, TweenInfo.new(0.2), {Position = UDim2.new(1, -22, 0.5, -10), BackgroundColor3 = v47.Main}):Play()
            v39:Create(Checkbox, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 50, 50)}):Play()
        else
            v39:Create(Indicator, TweenInfo.new(0.2), {Position = UDim2.new(0, 2, 0.5, -10), BackgroundColor3 = Color3.fromRGB(200, 200, 200)}):Play()
            v39:Create(Checkbox, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(40, 40, 40)}):Play()
        end
        callback(Toggled)
    end

    ToggleFrame.MouseButton1Click:Connect(function()
        Toggled = not Toggled
        UpdateToggle()
    end)

    UpdateToggle()
end

-- تجربة إضافة الـ Auto Equip لـ LP10 فـ الصفحة الرئيسية
CreateToggle(MainHash, "Strict Auto Equip (Slot 1)", false, function(Value)
    LP10_Settings.AutoEquip = Value
end)
-- [[ LP10 HUB | PART 8 ]]

-- وظيفة إنشاء زر عادي (Simple Button)
local function CreateButton(parent, text, callback)
    local Button = Instance.new("TextButton")
    Button.Name = text .. "Button"
    Button.Parent = parent
    Button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Button.Size = UDim2.new(0.9, 0, 0, 35)
    Button.Font = Enum.Font.GothamSemibold
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 14
    Button.AutoButtonColor = false

    local ButtonCorner = Instance.new("UICorner")
    ButtonCorner.CornerRadius = UDim.new(0, 6)
    ButtonCorner.Parent = Button

    local ButtonStroke = Instance.new("UIStroke")
    ButtonStroke.Parent = Button
    ButtonStroke.Color = v47.Main
    ButtonStroke.Thickness = 1
    ButtonStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    Button.MouseButton1Click:Connect(function()
        v39:Create(Button, TweenInfo.new(0.1), {BackgroundColor3 = v47.Main, TextColor3 = Color3.fromRGB(0,0,0)}):Play()
        wait(0.1)
        v39:Create(Button, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(35, 35, 35), TextColor3 = Color3.fromRGB(255,255,255)}):Play()
        callback()
    end)
end

-- وظيفة إنشاء سلايدر (Slider) للتحكم في السرعة مثلاً
local function CreateSlider(parent, text, min, max, default, callback)
    local SliderFrame = Instance.new("Frame")
    SliderFrame.Name = text .. "Slider"
    SliderFrame.Parent = parent
    SliderFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    SliderFrame.Size = UDim2.new(0.9, 0, 0, 50)
    
    local SliderCorner = Instance.new("UICorner")
    SliderCorner.CornerRadius = UDim.new(0, 6)
    SliderCorner.Parent = SliderFrame

    local SliderLabel = Instance.new("TextLabel")
    SliderLabel.Parent = SliderFrame
    SliderLabel.BackgroundTransparency = 1
    SliderLabel.Position = UDim2.new(0, 10, 0, 5)
    SliderLabel.Size = UDim2.new(0.8, 0, 0, 20)
    SliderLabel.Font = Enum.Font.GothamSemibold
    SliderLabel.Text = text .. " : " .. default
    SliderLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    SliderLabel.TextSize = 13
    SliderLabel.TextXAlignment = Enum.TextXAlignment.Left

    local SliderBar = Instance.new("Frame")
    SliderBar.Parent = SliderFrame
    SliderBar.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    SliderBar.Position = UDim2.new(0, 10, 0, 30)
    SliderBar.Size = UDim2.new(1, -20, 0, 6)

    local Fill = Instance.new("Frame")
    Fill.Parent = SliderBar
    Fill.BackgroundColor3 = v47.Main
    Fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    Fill.BorderSizePixel = 0

    -- منطق تحريك السلايدر (Logic)
    local UserInputService = game:GetService("UserInputService")
    local dragging = false

    SliderFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
            Fill.Size = UDim2.new(pos, 0, 1, 0)
            local value = math.floor(min + (max - min) * pos)
            SliderLabel.Text = text .. " : " .. value
            callback(value)
        end
    end)
end

-- إضافة أزرار تجريبية لـ LP10
CreateButton(MainHash, "Copy Discord Server", function()
    setclipboard(LP10_Discord)
    SendLP10Message("Discord", "Link Copied!")
end)

CreateSlider(SettingsHash, "WalkSpeed", 16, 200, 16, function(v)
    GetCharacter().Humanoid.WalkSpeed = v
end)
-- [[ LP10 HUB | PART 9 ]]

-- وظيفة الطيران (Fly System) معدلة بـ LP10 Style
local Flying = false
local FlySpeed = 50

local function StartFly()
    local char = GetCharacter()
    local root = GetRoot(char)
    local hum = char:FindFirstChildOfClass("Humanoid")
    
    local bv = Instance.new("BodyVelocity", root)
    bv.MaxForce = Vector3.new(1e6, 1e6, 1e6)
    bv.Velocity = Vector3.new(0, 0, 0)
    bv.Name = "LP10_Fly"
    
    task.spawn(function()
        while Flying do
            bv.Velocity = hum.MoveDirection * FlySpeed
            task.wait()
        end
        bv:Destroy()
    end)
end

-- وظيفة Kill Aura خفيفة (Combat)
local function KillAura(range)
    task.spawn(function()
        while LP10_Settings.KillAura do
            for _, v in pairs(game.Players:GetPlayers()) do
                if v ~= v37 and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                    local distance = (GetRoot(GetCharacter()).Position - v.Character.HumanoidRootPart.Position).Magnitude
                    if distance <= range then
                        -- هنا كتحط الـ Remote Event ديال الضربة على حسب اللعبة
                        -- مثلاً: game.ReplicatedStorage.HitEvent:FireServer(v.Character)
                    end
                end
            end
            task.wait(0.1)
        end
    end)
end

-- إضافة التبويب الخاص بالـ Combat (القتال)
CreateToggle(CombatHash, "Kill Aura (Cyan Mode)", false, function(v)
    LP10_Settings.KillAura = v
    if v then KillAura(20) end
end)

CreateToggle(CombatHash, "Infinite Jump", false, function(v)
    game:GetService("UserInputService").JumpRequest:Connect(function()
        if v then
            GetCharacter():FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
        end
    end)
end)

-- إضافة سلايدر لسرعة الطيران
CreateSlider(CombatHash, "Fly Speed", 10, 300, 50, function(v)
    FlySpeed = v
end)

CreateButton(CombatHash, "Toggle Fly (E)", function()
    Flying = not Flying
    if Flying then StartFly() end
    SendLP10Message("Movement", "Fly is now " .. (Flying and "ON" or "OFF"))
end)
-- [[ LP10 HUB | PART 10 ]]

local TeleportService = game:GetService("TeleportService")

-- وظيفة إعادة الدخول للسيرفر (Rejoin)
local function RejoinServer()
    TeleportService:Teleport(game.PlaceId, v37)
end

-- وظيفة التنقل بين السيرفرات (Server Hop)
local function ServerHop()
    local Http = game:GetService("HttpService")
    local Api = "https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Desc&limit=100"
    local _Servers = Http:JSONDecode(game:HttpGet(Api))
    for i,v in pairs(_Servers.data) do
        if v.playing < v.maxPlayers and v.id ~= game.JobId then
            TeleportService:TeleportToPlaceInstance(game.PlaceId, v.id, v37)
            break
        end
    end
end

-- إضافة أزرار السيرفر فـ صفحة الـ Settings
CreateButton(SettingsHash, "Rejoin Server", function()
    RejoinServer()
end)

CreateButton(SettingsHash, "Server Hop", function()
    ServerHop()
end)

-- وظيفة تعديل الـ FPS Cap (باش يخف السكربت)
CreateSlider(SettingsHash, "Max FPS Cap", 30, 240, 60, function(v)
    setfpscap(v)
end)

-- [[ تهيئة الـ ESP خفيف (رؤية اللاعبين عبر الجدران) ]]
local function CreateESP()
    for _, v in pairs(game.Players:GetPlayers()) do
        if v ~= v37 and v.Character and not v.Character:FindFirstChild("LP10_Highlight") then
            local Highlight = Instance.new("Highlight")
            Highlight.Name = "LP10_Highlight"
            Highlight.Parent = v.Character
            Highlight.FillColor = v47.Main -- Cyan
            Highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            Highlight.FillTransparency = 0.5
        end
    end
end

CreateToggle(CombatHash, "Player ESP (Cyan)", false, function(v)
    if v then
        CreateESP()
    else
        for _, pl in pairs(game.Players:GetPlayers()) do
            if pl.Character and pl.Character:FindFirstChild("LP10_Highlight") then
                pl.Character.LP10_Highlight:Destroy()
            end
        end
    end
end)
-- [[ LP10 HUB | PART 11 - THE FINAL PIECE ]]

-- وظيفة الـ Anti-AFK (باش تبقى كونيكطي وخا تكون غايب)
local function AntiAFK()
    local VirtualUser = game:GetService("VirtualUser")
    game.Players.LocalPlayer.Idled:Connect(function()
        VirtualUser:CaptureController()
        VirtualUser:ClickButton2(Vector2.new())
    end)
    SendLP10Message("Security", "Anti-AFK is now Active!")
end

-- زر تفعيل الـ Anti-AFK فـ الإعدادات
CreateButton(SettingsHash, "Enable Anti-AFK", function()
    AntiAFK()
end)

-- وظيفة إغلاق وفتح الواجهة (Toggle UI) بـ زر محدد (مثلاً RightControl)
local UI_Visible = true
game:GetService("UserInputService").InputBegan:Connect(function(input, processed)
    if not processed and input.KeyCode == Enum.KeyCode.RightControl then
        UI_Visible = not UI_Visible
        MainFrame.Visible = UI_Visible
    end
end)

-- [[ FINAL BOOT-UP SEQUENCE ]]
local function FinalBoot()
    -- أنميشن خفيفة لفتح السكربت
    MainFrame.Size = UDim2.new(0, 0, 0, 0)
    MainFrame.Visible = true
    v39:Create(MainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back), {Size = UDim2.new(0, 600, 0, 400)}):Play()
    
    -- صوت النجاح
    local bootSound = Instance.new("Sound", game.Workspace)
    bootSound.SoundId = "rbxassetid://138090596" -- صوت خفيف ناضي
    bootSound:Play()
    
    -- رسالة ترحيبية
    print("================================")
    print("💎 LP10 HUB LOADED SUCCESSFULLY")
    print("👤 Welcome, Boss " .. User)
    print("🛠️ Dev: " .. Dev)
    print("================================")
    
    SendLP10Message("Welcome", "LP10 HUB is ready for action!")
end

-- تشغيل السكربت كليا
FinalBoot()

-- دابا السكربت كامل مجموع عندك آ حمزة.
-- تقدر تجمع هاد الـ 11 Part فـ ملف واحد بالترتيب وغادي يخدم ليك ناضي بـ Cyan Theme و LP10 Branding.
